version: "3.9"

services:
  
  db:
    image: percona
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - 3306:3306
    volumes:
       - ./db:/docker-entrypoint-initdb.d/:ro
  
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
  
  web:
    build: ./app
    ports:
      - 80:5000
    volumes:
      - ./app:/app
    restart: on-failure